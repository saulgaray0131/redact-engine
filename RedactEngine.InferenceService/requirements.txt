fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20
numpy==2.2.4
opencv-python-headless==4.11.0.86
Pillow==11.2.1
# torch is intentionally NOT listed here. It is installed in the Dockerfile
# with a CUDA-specific wheel (torch==2.6.0+cu118). Listing torch as an
# unbounded >=2.6 requirement causes pip's resolver to "upgrade" to the
# latest PyPI torch — which silently replaces the cu118 wheel with +cu130
# and breaks GPU inference on the ACA T4 nodes.
# For local dev, install torch manually:
#   pip install torch==2.6.0  # CPU wheel from PyPI
# or use the CUDA index if you have a compatible GPU.
transformers==4.46.3
timm==1.0.11
accelerate==1.1.1
# SAM 2 for mask-level video tracking. Installed from GitHub; not published on PyPI.
# Requires `git` to be available in the build environment (see Dockerfile).
sam-2 @ git+https://github.com/facebookresearch/sam2.git@main
hydra-core>=1.3.2
